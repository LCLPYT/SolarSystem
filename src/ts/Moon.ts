/*
Dieses Modul definiert die Moon-Klasse.
*/

import { Body } from "./Body";

/**
 * Repräsentiert einen Mond. Wird im Moment nur für die Identifizierung gebraucht. 
 * Monde hinterlassen keine Spuren, weil diese Klasse nicht die OrbitBody-Klasse implementiert.
 */
export class Moon extends Body {

}