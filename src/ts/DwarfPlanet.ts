/*
In diesem Modul wird die DwarfPlanet-Klasse definiert.
*/

import { Planet } from "./Planet";

/**
 * Repräsentation eines Zwergplaneten. Dient bisher nur der Identifizierung.
 */
export class DwarfPlanet extends Planet {
    
}